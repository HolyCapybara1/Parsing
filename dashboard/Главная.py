import sys
import os

# Добавляем корень проекта в PYTHONPATH
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

import streamlit as st

st.set_page_config(
    page_title="ЦенМонитор",
    page_icon="📊",
    layout="wide",
    initial_sidebar_state="expanded",
)

from db.repository import init_db
init_db()

st.title("📊 ЦенМонитор — Система мониторинга цен конкурентов")
st.markdown(
    """
    Добро пожаловать в **ЦенМонитор** — систему автоматического сбора и анализа цен
    интернет-магазинов электроники (DNS, Ситилинк, Regard).

    **Выберите раздел в левом меню:**
    - **Главный дашборд** — обзор рынка и ключевые метрики
    - **Аналитика** — динамика цен и ценовой индекс
    - **Сравнение категорий** — сводная таблица по категориям
    - **ML-сегментация** — кластеризация товаров по ценовым сегментам
    - **Все товары** — полная таблица с поиском и экспортом
    """
)

st.info(
    "Если данных ещё нет — перейдите на страницу **Главный дашборд** "
    "и нажмите кнопку **Собрать данные**."
)
